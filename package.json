{
  "name": "lyrics-chord-detector",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
